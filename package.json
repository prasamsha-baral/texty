{
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "serve": "bun --filter 'backend' serve",
    "start": "bun run build && bun run serve"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
